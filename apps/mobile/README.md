# apps/mobile (Flutter)

Placeholder de estructura. `pubspec.yaml`, `main.dart` y las carpetas de plataforma (iOS/Android/etc.) todavía
NO se han generado — deben crearse con `flutter create` (ver `docs/Grasp-Fase1.md` § Bloque 0) antes de que este
paquete sea ejecutable. La arquitectura por capas (`core/data/domain/features`) ya está scaffoldeada.
