<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- INTERNET vivia solo en los manifests de debug y profile, que es lo que trae la plantilla de
         Flutter para el hot reload. En release no lo declaraba nadie, y esta app no hace nada sin
         red (Supabase, R2, LiveKit). Se declara aqui para no depender de que lo aporte por fusion
         el manifest de alguna dependencia. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Voz en tiempo real (LiveKit / flutter_webrtc). Solo audio: NO se declara CAMERA, aunque
         flutter_webrtc la soporte, porque Grasp no tiene video ni lo va a tener. MODIFY_AUDIO_SETTINGS
         es para enrutar al altavoz/auricular, y BLUETOOTH_CONNECT (API 31+) para los auriculares.
         El permiso de micro ademas se pide en tiempo de ejecucion; eso vive con la pantalla de sala. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>

    <!-- image_picker NO necesita ningun permiso aqui, y es deliberado: la hoja del avatar solo
         abre el selector de fotos del sistema (ImageSource.gallery), que en Android 13+ ya no
         exige READ_EXTERNAL_STORAGE. Y no se declara CAMERA porque la app no ofrece hacerse la
         foto en el momento. Si algun dia se anade esa opcion, ese sera el momento de pedirlo:
         declarar permisos que no se usan da fricción en la revision de la tienda y desconfianza
         a quien instala. -->

    <application
        android:label="grasp_mobile"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
